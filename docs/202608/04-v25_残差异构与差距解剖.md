# v25:残差异构学习证伪 + 冲第一差距解剖

## Analysis

用户明确目标"想到第一名"。两线并行:①残差异构学习(自有创新最后一项);②**差距解剖**——距第 1 名 0.0189(大于 8 天全部战果 −0.0151),必须先定位差距藏在哪个误差分量,才谈得上打法。冠军帖留有两个可对标硬数字:clf AUC 0.914、无 outlier 回归 CV 1.545。

## Progress

- [x] **v25 res-GRU:❌ 证伪(+0.00004)**。v13 骨架改学融合残差:残差 OOF 相关仅 **0.0066**,直接校正 3.62056(-0.00006)。融合残差对异构载体是白噪声——v8 定律对照成立,自有创新清单出清;
- [x] **差距解剖(三步)**:
  1. 误差质量:融合 OOF 平方误差 **74.4% 在 outlier 侧**(2207 张卡,1.09%);
  2. 组件对标:**非 outlier 回归 1.5473 ≈ 冠军 1.545(打平**,oracle 模拟收益仅 +0.0003);**clf AUC 0.9059 < 冠军 0.914(唯一缺口)**;
  3. 分类器价值模拟:线性混真值法穿帮(代数可反解,Δ 恒等完美值,弃);改合成独立分类器替换法——同 AUC sanity 对照 +0.18(合成噪声与成员误差不相关,绝对值不可用),**边际量 0.906→0.914 ≈ +0.025,与实际差距 0.019 同量级吻合**。

## Review

**到第一名的路径是唯一的:outlier 分类器 AUC 0.906 → 0.914+,且提升须落在极端尾部。**

1. 非 outlier 侧已与冠军打平,树系特征/融合层/NN 回归通道全部出清——**其余一切方向的天花板都是 0.0005 量级,只有分类器缺口是 0.02 量级**;
2. 冠军的 0.914 来自行级元特征私有细节,公开复现已证不可行(v8 三用法证伪 + 28th 复现 0.9089 无提升 + 冠军帖多人 "tried didn't work");
3. v15 警示:clf ens AUC +0.002(→0.9078)三用法均不转化 —— **AUC 不是正确的靶指标**,RMSE 只关心尾部排序质量(真 outlier 的 p_cal 是否够高),改进必须用"替换 f_clf 后融合 Δ"直接度量;
4. 唯一有量级匹配的未试手段:**2026 年代方法攻分类任务**——3000 万行交易自监督预训练(BERT式)+ outlier 微调,2019 年不存在、榜单从未出现、v17 的失败是回归任务(不适用于分类先验)。属研究级投入(GPU 数天),成功率低但杠杆对口。

项目现状仍为 v14 第 7 名;冲第一 = 单点攻坚分类器尾部,别无他路。
