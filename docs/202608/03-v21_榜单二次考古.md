# v21:榜单二次考古(11th/18th/32nd/7th-late + xentropy/w2v)

## Analysis

用户推动对榜单做第二轮考古。Meta Kaggle 检索发现上轮审计(只覆盖 #1/#5/#7/#10 的 place-solution 主帖)之外还有:**11th 完整方案、18th 故事帖、32nd diversity 帖、"simple trick" 69 楼长帖、xentropy 讨论帖、test outlier 估计帖**。

### 考古净新信息
1. **xentropy 目标**(1st/7th 均用;"binary better than regression even when metric is rmse"):target MinMax→[0,1] 后按交叉熵目标训练,对 outlier 端梯度形状与 L2/Huber 完全不同 —— 我们池缺失的**目标函数轴**(现有 L2×3/Huber/MSE)。跟帖实测者:"private +0.001 但 CV 不变"(有效但弱信号);
2. **访问序列 embedding**(11th "visit sequence to sequence embedding" + 7th w2v):商户/品类访问序列共现结构,树与 GRU 均未吃过。gensim 环境不可用 → 用 **PPMI-SVD**(SGNS 的谱等价)实现;
3. **11th 后处理实盘**:top-100×4 分类器交集硬替换,LB 3.675→3.666 大涨,**Private 纹丝不动(3.599=3.599)** —— 从他人实盘二次验证我们对 topN 替换家族的证伪;
4. **18th 教训**:选了 post-process 版最终提交,损失约 8 个名次("no-pp 本可第 10");其 ⑤"时间预测残差"(辅助模型预测 new 窗末次交易时点,取残差)为未试过的"模型生成特征"(中先验,备选);
5. **32nd diversity 轴清单**:损失函数多样性(poisson——target 含负,不可直接用)、**小特征子集成员**(20-60 列/模型,备选)、target 5 类多分类(池大时无用,弃);
6. test outlier 率 ≈ train(1.09%),无直接可用。

### 立项
- **A `v21_xe.py`**:LGB xentropy 成员(10 折 777,逆变换)→ 同池对照判据(F31 33 成员 ± e_xe,bayes,ΔOOF>0.0005);
- **B `v21_seqemb.py`**:同卡交易序列窗口 ±3 的 merchant / merchant_category 共现 → PPMI → TruncatedSVD(16/8 维)→ 金额加权卡向量 24 列 → 树单模判据(vs base_fm/lgb 3.63246)。

## Progress

- [x] **A xentropy:❌ 不足(+0.00004)**。单模 OOF **3.63147**(超 f_lgb 0.001,1st/7th 配方单模层面为真);但与 L2-LGB 预测相关 **0.9922** —— 同特征空间换目标函数,学出的函数几乎相同。**目标函数轴只在浅池中构成异构**;32 成员深池下边际归零(与 v20 降噪不叠加同构的"深池吸收定律");
- [x] **B seq-emb:❌ 证伪(−0.00065)**。24 列 PPMI-SVD 卡向量 0 列进 gain 前 50 —— 词向量金额加权平均抹平序列结构后,剩余"商户类型偏好"信息已被品类 TE/mode 覆盖。11th 原文的 seq2seq 编码器形态需 NN 级引入,受 v18 载体定律压制,不追;
- [x] 备选出清判定:18th 时间预测残差(特征族,v10/v12/v16 三连证伪同类)、32nd 小特征子集成员(精度下限 + 深池吸收双重压制)——预期边际 <0.0002,不烧。

## Review

**榜单二次考古:检索有真产出(6 个未读帖),判据全数拦截,第 7 名收敛结论经受第二次推动检验。**

考古的独立价值(不依赖判据):
1. **11th 实盘**:top-100×4 交集硬替换 LB +0.009、**Private 0.000** —— 他人实盘二次验证 topN 替换家族(公榜过拟合);
2. **18th 实盘**:选了 post-process 提交损失 ~8 名("no-pp 本可第 10")—— 后处理危险的第三方样本;
3. 11th/32nd 均 **NN 全线失败** —— 我们 v13/v14 的 NN 落地是相对榜单同段位队伍的真实优势;
4. 新定律:**深池吸收** —— 单模层面为真的改进(xe 3.63147)在深池融合层归零;浅池时代的榜单配方(xentropy/多 seed/小子集)对 32 成员深池全部失效。

至此:前十 4 主帖(上轮)+ 二轮 6 帖 + 11-32 名可得方案,**榜单公开信息全部出清**。项目定格私榜等效第 7 / 4111。
