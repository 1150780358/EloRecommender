# v22:低先验备选实测出清(18th 时间残差 / 32nd 小特征子集)

## Analysis

v21 二次考古后剩两个"判定不烧"的备选,用户推动实测,把"预期边际 <0.0002"变成实测数字,完成榜单信息的最后出清:

1. **A. 18th 时间预测残差**(特征族):辅助 LGB 用 hist-only + 静态特征预测"new 窗末次交易距锚点天数",残差 = 实际 − 预测,刻画"回访比预期早/晚"。同类(时间结构特征)已被 v10/v12/v16 三连证伪,先验低;
2. **B. 32nd 小特征子集成员**(成员族):20-60 列/模型的多样性配方。受精度下限 + 深池吸收双定律压制,先验低。

## Plan

- **A `v22_timeres.py`**:aux 目标 = REF_DATE(2018-05-01) − 卡末次 new 交易日(天);aux 特征 = 不含 new 信息的列(hist_/dec_/静态,排除 x_/newlag);5 折 OOF 化避免残差过拟合;主模型 = v21 同款 X(sel+TE+td+fm)+ tp_last/tp_pred/tp_resid 3 列。判据:单模 vs base_fm/lgb(3.63246),Δ>0.0005;
- **B `v22_subsets.py`**:4 个 40 列 LGB 成员——s1/s2/s3 = gain 排名 1-40/41-80/81-120 段,s4 = 全列随机 40(seed 777)。判据:F31 33 成员池 bayes 对照,+4 成员及逐一,Δ>0.0005。

## Progress

- [x] **A timeres:❌ 不足(+0.00031)**。aux 模型 OOF 误差 14.5 天(覆盖率 89%),tp_resid 冲进主模型 gain 第 7 —— 特征本身是真的("回访比预期早/晚"确有信号);但单模 OOF 3.63216,改善 +0.00031 < 0.0005。入池补查(s4+tp)也只 +0.00005 —— 双层判据一致拦截;
- [x] **B subsets:判据通过但线上不转化(首次误报)**。四子集成员单模 3.66-3.69、与主 LGB 相关 0.85-0.89(异构性远超 xentropy 的 0.9922);融合层穷查最优 s1+s2 Δ=+0.00072 > 0.0005 → 按纪律提交;
- [x] **线上实测:Private 3.59910(vs v14 3.59764,恶化 +0.00146)** —— ΔOOF −0.00072 完全不转化且反向。v14 仍为最优,未损失任何名次(Kaggle 按 best 计)。

## Review

**两个低先验备选实测出清;B 路制造了判据系统 10 次通过中的第一次误报,由此获得本项目最后一条定律。**

1. **新定律:同源成员的融合增益是融合层过拟合**。e_s1/e_s2 是同一特征矩阵的子集上训练的 LGB —— 预测相关低(0.85)但**不携带任何新信息**,其"异构性"是伪的:融合层在折内利用了它们与主模型不同的误差模式,而这些误差模式(子集欠拟合噪声)不泛化。回看 9 次成功转化,全部携带真新信息(TE/TD/FM)或真异构载体(NN);判据 Δ>0.0005 这一单一数值门槛,必须与「新信息 × 异构性」定性判据**联合使用**——v21 之前从未单独依赖过数值门槛,这次是第一次,也立即被惩罚;
2. **判据战绩修正:10 发提交 9 转化 1 误报**;误报的方差代价为零(best 计分),信息收益为一条定律 —— 这次提交是花 1 次额度买到"数值判据不充分"的实证,值;
3. A 路 tp_resid(gain 第 7 但 +0.00031)与 18th 原帖"辅助模型特征"叙述吻合:真信号、小边际,与 v10/v12/v16 特征族三连证伪同段位;
4. **项目最终定格不变:v14,私榜等效第 7 / 4111(3.59764)**。至此数值判据通过的候选也已线上验证出清,收敛结论从"判据拦截"升级为"线上实测确认"。
