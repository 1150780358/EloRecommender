# v27:自监督预训练 clf(冲第一的最后一发)

## Analysis

v25 解剖:差距 0.019 全在 clf 尾部(0.906 vs 冠军 0.914);v26 出清了 clf 的全部公开路线。
剩余唯一量级对口手段 = **2019 年不存在的方法**:交易序列自监督预训练 + outlier 微调。
- 先验支持:预训练把 3000 万无标签交易的结构信息注入编码器,理论上正攻 2207 正例的小样本瓶颈;载体与 LGB 彻底异构;
- 先验反对:v15(NN clf ens 0.9078 不转化)、v17(交易级序列回归失败,但属回归任务不构成分类先验)。

## Plan

`src/v27_ssl.py` 复用 v17 tx_tensor 缓存(近 128 笔 ×7 数值 + mcat/ssec + 静态):
1. **Stage1 masked 预训练**:325k 卡全量(无标签无泄漏),Transformer(4L,d96,4头),遮 15% 真实交易预测 mcat(CE)+金额(MSE),3 epoch;
2. **Stage2 逐折微调**:777 十折,预训练权重 + mean-pool + 静态分支 + 二分类头,pos_weight 平衡,早停看 val AUC;
3. **判据链**:①OOF AUC vs 0.90586;②rank 混合 f_clf 最优 AUC;③融合层(追加成员 / f_clf 替换为混合)Δ>0.0005。

## Progress

- [x] **A 裸序列版:❌**。预训练收敛正常(masked mcat acc 26.75%,330 类词表);微调 OOF AUC **0.82799** —— 裸序列不敌 334 列工程特征;rank 混合 w=0.05 AUC 0.90615;融合追加 +0.00000 / 替换 −0.00101;
- [x] **B 全量特征版:❌**。静态分支换 334 列标准化特征:OOF AUC **0.89217**;混合 w=0.20 **AUC 0.90700**;融合替换 **+0.00026** / 追加 −0.00002;
- [x] **C 被拒交易版:❌**。churn 机制(卡冻结→连拒→流失)驱动的张量重建(8 通道含 is_denied,预训练加被拒预测头):OOF AUC **0.89369**(+0.0015);三方混合(lgb .75/sslB .10/sslD .15)**AUC 0.90734 = 项目历史最高分类器**;融合替换 **+0.00022** / 追加 −0.00002。B→C 边际衰减(混合 +0.0003,融合 Δ 反降)。

## Review

**冲第一的最后一发打完:三迭代全部不足,SSL 路线在混合 AUC ~0.907 平台收敛,判据封顶 +0.0003。**

1. 预训练本身是成功的(结构学习收敛、被拒通道有真信息、混合 AUC 创项目新高)——**失败的不是技术,是信息量**:第四条独立路线(LGB 特征 / NN ens / SSL / SSL+denied)全部收敛到 0.906-0.907,这是四重证据下的**数据信息论天花板**;
2. v15 定律第三次应验:中段 AUC 提升(+0.0015)不转化,RMSE 只认尾部质量;
3. 冠军 0.914 与我们天花板 0.907 之间的 0.007,在穷尽公开配方(v8/v26)与超时代方法(v27)后仍不可达 —— 最终定性:**私有洞察,不可复现**;
4. 冲第一战役(v25-v27)总结:差距 0.019 定位于单点(clf 尾部)→ 公开路线出清(v26)→ 超时代路线出清(v27)。**第 7 名是该数据 + 诚实方法论 + 公开信息的完整答案。**

