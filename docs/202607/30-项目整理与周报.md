# 项目整理与第 1 周周报

- **日期**:2026-07-30
- **任务**:① 清理并重组项目目录结构 ② 补齐 README ③ 产出第 1 周正式周报

## Analysis

- 现状:14 个 Python/Shell 脚本平铺项目根目录,`__pycache__` 混入;`outputs/` 下 12 个 nohup 日志与数据产物混放;无 README,复现者无从下手;`.gitignore` 已就绪但尚未 `git init`。
- 约束:全部实验脚本 `import elo_pipeline`(同目录依赖),并以相对路径读写 `data/`、`outputs/`、`docs/` —— 移动时必须保持同目录、约定从项目根运行;无脚本使用 `__file__` 定位,整体搬移安全。
- 已确认无正在运行的 Elo 相关进程(后台 python 任务均属其他项目),可以动文件。

## Plan

1. 新建 `src/`(全部 .py,含 elo_pipeline.py)与 `scripts/`(战役 .sh);.sh 更新内部路径并加 `cd` 到项目根的保护;
2. `outputs/*.log` → `outputs/logs/`,`.gitignore` 增补 `outputs/logs/`;删除 `__pycache__`;
3. 同步各脚本 docstring 中的用法命令(`python src/...`);
4. 验证:src 模块可导入、sh 语法检查通过;
5. 写 `README.md`(简介 / 成绩 / 结构树 / 复现指南 / 成绩口径);
6. 产出 `docs/周报/第1周-0727_0802.md`,并在 `docs/实习周报计划.md` 顶部挂链接。

## Progress

- [x] `src/`(11 个 .py)、`scripts/`(2 个 .sh)、`outputs/logs/`(13 个日志)、`docs/周报/` 建立并搬移完成;`__pycache__` 删除
- [x] `.sh` 内部路径(`$PY src/*.py`、`outputs/logs/campaign*.log`)与各 .py docstring 用法命令同步更新
- [x] `.gitignore` 增补 `outputs/logs/`
- [x] README.md / requirements.txt:发现为并行会话于 12:58-13:00 刚生成(内容准确),予以保留,仅将「文件地图」与「复现」两节更新为新目录结构
- [x] 验证:`bash -n` 两个战役脚本通过;`sys.path.insert(0,'src')` 后 `import elo_pipeline` 正常
- [x] 正式周报 `docs/周报/第1周-0727_0802.md` 定稿(覆盖至 07-30),并在 `实习周报计划.md` 顶部挂链接

## Review

- **结构**:根目录从 14 个平铺脚本收敛为 `src / scripts / data / outputs / refs / docs` 六目录 + README/requirements/.gitignore 三文件;约定「一律从项目根运行 `python src/xxx.py`」(src 内脚本同目录互相 import,依赖此约定)。
- **风险点**:本次发现另一会话在同时写文件(README、requirements、.gitignore 均非本会话产物),多会话并行操作同一仓库时需先看 mtime 再写,避免互相覆盖。
- **遗留**:仓库尚未 `git init`(.gitignore 已就绪,含 API 密钥排除项);周报覆盖至 07-30,余下三天如有新结果需在 §三 追加。
