# v7 战役:21st 剩余思路收尾 + Top1 文字复盘复现

- **日期**:2026-07-31
- **起点**:v6 F20(TE-v1),Private 3.60635,私榜等效第 59 名 / 4111;距金牌线(18 名,3.60078)差 0.00557
- **背景**:v5/v6 已确认「新信息是唯一有效路径」。21st 代码里还剩 2 个未验证的新信息候选;Top1(30CrMnSiA/砍手豪)只有文字复盘,需联网挖掘后复现其可实现部分。

## Analysis

- 机器空闲(64 核 / 负载 0.6 / 内存余 113G),无在跑 Elo 进程,三线可并行。
- **A 线:交易粒度时间差分布**(21st `zxs/pre_0214.ipynb` cell 43/50):每笔交易算
  `a2p`(开卡→购买天数)、`p2r`(购买→参考日)、`p2now`(购买→REF_DATE),
  按卡聚合 mean/median/max/min/std,hist/new 双表 = 30 列。我们现状只有卡粒度首笔/末笔,无分布统计。
- **B 线:弱正则参数对照**(21st `zxs/reg_single_cv3.63480.ipynb`):
  21st `leaves 31 / min_leaf 30 / max_depth -1 / L1 0.1 / 无 L2 / ff 0.9 / bf 0.9` vs
  我们 `leaves 63 / depth 8 / min_leaf 150 / L1 1.0 / L2 10 / ff 0.75 / bf 0.85`。
  同特征集(sel 249 + TE-v1 36)只换参数,零实现成本。
- **C 线:Top1 复盘挖掘**:refs/ 无一手材料,派研究代理抓 Kaggle discussion + 知乎复盘,
  产出可离线复现的招数清单(重点:outlier 精细建模、参考月后窗口行为特征、多层融合结构)。
- 判据沿用 v6:单模 lgb OOF 改善 >0.0005 才继续;>0.003 触发泄漏警报线(submissions.md 纪律)。
- 基线:`outputs/base_te/lgb.npz` 单模 OOF **3.64170**(sel+TE-v1);折划分 `ep.make_folds(y)` 与 v6 完全一致。

## Plan

| 线 | 内容 | 判据 | 状态 |
|---|---|---|---|
| A | `src/v7_timediff.py feat+lgb`:30 列时间差分布 → 单模 lgb | ΔOOF > 0.0005 | 进行 |
| B | `src/v7_timediff.py weakreg`:21st 参数对照 | ΔOOF > 0.0005(且警惕弱正则过拟合假象) | 进行 |
| C | 研究代理挖 Top1 复盘 → 招数清单按预期价值排序 | 产出可实现清单 | 进行 |
| D(后续) | A/B 任一通过 → 全套基模型 + F20 融合重跑 → 线上验证 | 融合 OOF、Private | 待 A/B |
| E(后续) | C 线清单择优实现 | 单模判据 | 待 C |

## Progress

- [x] 侦察:21st 两个 notebook 的一手实现与参数已提取;机器/环境确认可跑
- [x] `src/v7_timediff.py` 实现(A 线 feat+lgb / B 线 weakreg 双模式);TE-v1 缓存(36 列)与基线 OOF 3.6417 核验一致
- [x] **A 线:通过,且为本项目单模最大增益**。30 列时间差分布 → 单模 lgb OOF **3.63520**,改善 **+0.00650**(TE 的 5 倍)。gain 头部:`new_td_p2r_min`(断层第一)、`new_td_p2r_mean`、`new_td_a2p_std`。超 0.003 警报线,按纪律先审计:
  - ① 对抗验证(仅 td 30 列区分 train/test):AUC **0.4974** —— 分布完全一致,无 v6-v2 型错配;
  - ② 逐折 Δ:**10/10 折全部改善**(+0.0009 ~ +0.0146),非个别折运气;
  - ③ `new_td_p2r_min` 与观察月 spearman 仅 −0.06,固定观察月后组内仍有净关联;outlier 卡的 new 交易时间分布系统性不同(中位数 −33 vs −50 天,缺失率 26% vs 11%)——"outlier 卡参考月后行为异常"的直接行为信号;
  - 机制核验:td 特征不含 target、不依赖折划分,泄漏通道不存在。**判定:真实新信息,放行 D 阶段。**
- [x] **B 线:证伪**。21st 弱正则参数(leaves 31/min_leaf 30/无 L2)同特征集十折 OOF **3.64656**,比基线 3.64170 **差 0.00486**。早停轮数翻倍(≈1000-2300)但泛化更差 —— 我们的强正则参数在 285 维特征集上占优;21st 的单模 CV 优势应归因于其特征集与乐观 CV 口径,参数风格不可移植。工件 `outputs/base_td/lgb_weakreg.npz`
- [ ] A 线:时间差特征已生成(`outputs/td_features.parquet`,30 列/32.5 万卡,76s),单模 lgb 训练中(日志 `outputs/logs/v7_td.log`)
- [ ] B 线:弱正则参数对照训练中(日志 `outputs/logs/v7_weakreg.log`)
- [x] **D 阶段:线上验证通过,突破金牌线**。td 版全套基模型:xgb **3.63469**(历史最佳单模)、cat 3.64008(TE 曾使 cat 变差,td 反而改善 —— 不与其内建目标编码冲突)、clf AUC **0.90507** 新高。融合 F23 全池(TD+TE+原版,22 元特征,BayesianRidge)**OOF 3.62711**(较 v6 F20 −0.00481)。
  线上:Public 3.67880 / **Private 3.60064 → 私榜等效第 16 名 / 4111,越过金牌线(18 名,3.60078)**。
  ΔOOF −0.00481 → ΔPrivate −0.00571,**转化率 119%**:折外无依赖的行为特征在 test 上无任何口径损耗,转化率高于 TE(45%)。
- [x] **C 线:Top1 一手资料挖掘完成**,见 `docs/202607/31-Top1方案挖掘报告.md`。核心结论:Top1 无开源仓库,唯一 writeup 是 Kaggle #82036(经 Meta Kaggle 数据集拿到原帖+69 评论+冠军全部发言);期望式融合我们已覆盖(`ev` 元特征),真正缺的是**交易行粒度元特征**(冠军亲口指认的"most important features",其 AUC 0.914 vs 我们 0.90507)→ 立项 v8。

## Review

> **⚠️ 2026-07-31 口径修正(并入并行会话《31-v7-seed错配警告》)**:本战役全部运行未设 `ELO_SEED`,
> 用了默认 2019 折,而 TE 缓存/基线产物均为 777 折 —— **本文所有 OOF 数字含折协议错配的乐观偏置**
> (实证量级 ~0.0005,见警告文档)。**线上 Private 3.60064 为 Kaggle 真实打分,不受影响**;
> A 线结论有线上 −0.0057 背书、B 线在享受同侧偏置下仍败,两个定性结论均维持。
> 处置:`ELO_SEED=777` 全链重跑 base_td 与融合,恢复规范 OOF 基线(v9 判据以复跑值为准)。

**当前最优:v7 F23 全池融合,Private 3.60064,私榜等效第 16 名 / 4111 —— 金牌区(top 0.4%)。**
单日从 59 名(银)推进到 16 名(金),v7 是本项目最大单次跃升(ΔPrivate −0.00571)。

净结果:一个重大有效改动(时间差分布,+43 名次)、一个证伪(弱正则参数)、C 线待归。

方法论增量:
1. **泄漏警报线的正确用法**:A 线 OOF 改善超警报线后没有直接上线,而是走了三重审计(对抗验证 / 逐折一致性 / 机制核验)——警报线是"先审计"的触发器,不是"一律不上"的封禁线。真泄漏(v6-v2)与真信号(v7-A)在审计下可分。
2. **转化率与特征机制的关系**:折依赖的编码类特征(TE)转化率 45%,折外无依赖的行为特征(td)转化率 119% —— train/test 计算口径完全一致的特征,OOF 收益几乎无损转化。评估新特征优先级时,"口径是否折依赖"应作为预期转化率的先验。
3. **同一来源的招数价值差异极大**:21st 的三个未移植点,时间差分布(+0.0065)>> TE(+0.0012)>> 弱正则参数(−0.0049)。挖方案要挖到底,不能浅尝辄止后按二手印象排优先级。

遗留:C 线 Top1 清单待归;下一目标第 10 名 3.59904(差 0.0016),候选:C 线产出、w2v/node2vec 序列嵌入、NN/NFFM(低优先)。
